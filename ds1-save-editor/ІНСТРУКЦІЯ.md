# DS1 Save Editor - Інструкція

## 🚀 Для користувачів (без npm)

### Варіант 1: Готова програма (найпростіше!)

Скачайте готовий executable файл з [Releases](../../releases):
- **Windows**: `DS1-Save-Editor-Portable.exe` - просто запустити
- **Linux**: `DS1-Save-Editor.AppImage` - дати права і запустити
- **macOS**: `DS1-Save-Editor.dmg`

**Не потрібно встановлювати Node.js або npm!**

### Варіант 2: Запуск через батник (потрібен Node.js)

1. Встановіть [Node.js](https://nodejs.org/)
2. Подвійний клік на **`RUN-ELECTRON.bat`**
3. Почекайте поки зберутся файли
4. Програма запуститься автоматично

## 🛠 Для розробників

### Швидкий старт

```bash
# Встановити залежності
npm install

# Розробка (веб версія з hot reload)
npm run dev

# Тестування Electron
npm run electron

# Створити executable
npm run dist         # Для вашої ОС
npm run dist:win     # Тільки Windows
```

### Доступні команди

| Команда | Призначення |
|---------|-------------|
| `npm run dev` | Vite dev сервер (http://localhost:5173) |
| `npm run build` | Білд для Cloudflare Pages |
| `npm run build:static` | Білд для Electron |
| `npm run electron` | Зібрати і запустити Electron |
| `npm run electron:dev` | Запустити Electron без білду |
| `npm run dist` | Створити executable для поточної ОС |
| `npm run dist:win` | Windows .exe файли |
| `npm run dist:linux` | Linux AppImage та .deb |
| `npm run dist:mac` | macOS .dmg |

### Деплой на Cloudflare Pages

```bash
npm run build
wrangler pages deploy ./ds1-save-editor/dist
```

## 📦 Результати білду

- **`dist/`** - Збілдовані веб файли
- **`release/`** - Executable файли (.exe, .AppImage, .dmg)

## ❓ Типові питання

**Q: Як створити portable .exe файл?**
A: `npm run dist:win` створить як installer, так і portable версію

**Q: Чи можна запускати без інтернету?**
A: Так! Electron версія працює повністю offline

**Q: Де зберігаються збереження Dark Souls?**
A: `%USERPROFILE%\Documents\NBGI\DARK SOULS REMASTERED\<user_id>\DRAKS0005.sl2`

**Q: Чи безпечно редагувати сейви?**
A: Завжди робіть бекап файлу перед редагуванням!

## 🎯 Різниця між білдами

### Web білд (`npm run build`)
- Для хостингу на Cloudflare Pages / GitHub Pages
- Абсолютні шляхи (`/assets/...`)
- Потребує веб-сервер

### Static білд (`npm run build:static`)
- Для Electron
- Відносні шляхи (`./assets/...`)
- Працює через file:// протокол

### Electron білд (`npm run dist`)
- Standalone executable
- Не потребує Node.js для запуску
- Windows, Linux, macOS

## 📝 Ліцензія

MIT - дивіться LICENSE.txt
